{
  "name": "@pharos/corpus",
  "version": "0.1.0",
  "private": true,
  "description": "Pharos shared corpus module: D1 schema migrations + typed CorpusClient. Imported by paid-tier writer services (audit-fulfill, future Implementation, future AutoPilot). Architectural privacy guarantee per pharos-corpus-layer-spec.md §2.4: scanner Worker MUST NOT bind this database.",
  "main": "src/client.ts",
  "types": "src/client.ts",
  "exports": {
    ".": "./src/client.ts",
    "./types": "./src/types.ts",
    "./version": "./src/version.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260425.1",
    "typescript": "^5.7.0"
  }
}
