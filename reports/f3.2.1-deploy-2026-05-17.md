# F3.2.1 `cancel_pending_at` Column — 2026-05-17

**Scope.** Closes the pre-public-launch blocker flagged in F3.2 ship-report: cancel-click acknowledged by Dodo, but D1 `subscriptions.status` stayed `'active'` until period_end (Dodo doesn't fire `subscription.cancelled` on `cancel_at_next_billing_date` flips — only at actual period_end per F3.2 V-B observation). Page refresh during that window showed "Active + Cancel button" → customer confusion → trust gap on a $149/mo product. CLI v1 review surfaced a symmetric trust-gap on the reactivate side (already-active short-circuit would silently no-op for cancel-pending state) → spec v2 D3 expansion to triple-coordinated SELECT + short-circuit gating + UPDATE clear. Marketing-site worker `db931fa8-9e9f-4d30-b3c2-afe4099882ca`; no citation-tracking deploy (migration-only).

## Files created
- `citation-tracking/migrations/0007_cancel_pending_at.sql` (D1 — single ALTER TABLE ADD COLUMN INTEGER; nullable; no backfill; pattern matches F3.1 migration 0006 `audit_fired_at` precedent)

## Files modified
- `marketing-site/src/app/api/account/cancel/route.ts` (D2 — SET cancel_pending_at = now() after Dodo PATCH success; failure-tolerant try/catch per CLI v1 MED-2)
- `marketing-site/src/app/api/account/reactivate/route.ts` (D3 triple-coordinated per spec v2 — SELECT extended; short-circuit gated on cancel_pending_at === null preserving next_billing_date field; UPDATE clearing cancel_pending_at = NULL after Dodo PATCH success with try/catch)
- `marketing-site/src/app/api/dodo-webhook/route.ts` (D4 + D5 — subscription.cancelled + subscription.reactivated UPDATE statements extended with cancel_pending_at = NULL alongside existing status restores)
- `marketing-site/src/app/account/page.tsx` (D6 — SubscriptionRow extended; SELECT extended; AccountView derivation gains early-precedence `if (sub.cancel_pending_at !== null && sub.status === "active")` branch ordered FIRST; existing `if (sub.status === "active")` converted to `else if` to preserve single-precedence chain per CLI v2 LOW-NEW-1 absorption)

## V-read resolutions (execution-time)
- **V-A**: 14 columns confirmed pre-migration; cancel_pending_at NOT present (idempotency check); audit_fired_at at cid=13 per F3.1 migration 0006. Post-migration: 15 columns, cancel_pending_at at cid=14 (INTEGER, notnull=0, dflt_value=NULL).
- **V-B**: cancel route structure confirmed (PATCH true + idempotency on status='cancelled'); insertion point between log line and final return.
- **V-C inline absorption (CLI v2 MED-NEW-2)**: deploy-prompt v2 V-C claimed actual SELECT was 6-column wide; **real SELECT is narrow** (`SELECT status, current_period_end FROM subscriptions WHERE subscription_id = ?`). Inline absorption: extended minimally to 3 columns (status + current_period_end + cancel_pending_at), not the 7-column shape from the prompt's snippet. Avoided 4 gratuitous unused columns.
- **V-D**: webhook dispatch confirmed named-events shape (subscription.active + subscription.renewed + subscription.cancelled + subscription.reactivated + log-only failed/on_hold/plan_changed); pivot path `subscription.updated` was never activated per F3.2 V-D resolution.
- **V-E**: /account AccountView if/else if chain confirmed; SubscriptionRow interface confirmed. Early-precedence branch inserted FIRST.
- **CITATION_DB cross-binding**: present in marketing-site/wrangler.jsonc per F3.1 carryover.

## Deploy
- Marketing-site worker version: `db931fa8-9e9f-4d30-b3c2-afe4099882ca`
- No citation-tracking source changes — only migration 0007 applied to remote + local D1
- No new secrets bound; no new Workers; no new endpoints
- `npm run cf:deploy` (marketing-site) completed clean
- Smoke check: `curl astrant.io/llms.txt | findstr Astrant` — PASS

## Phase 1-5 verification (executed 2026-05-17 post-deploy)

All five phases pass ✓ on Bruno's test subscription `sub_0NevthLtkMQcpP5QlKFIx`:

**Phase 1 (schema verification)**: PASS. PRAGMA table_info(subscriptions) shows 15 columns; cancel_pending_at at cid=14 with notnull=0, dflt_value=NULL. Baseline count of pending subs: 0.

**Phase 2 (cancel sets cancel_pending_at)**: PASS. Cancel click → modal Confirm → POST /api/account/cancel → 200 + next_billing_date. D1 query confirms `cancel_pending_at=1779056419` populated + `status='active'` (Dodo did NOT webhook on the flag flip, per F3.2 V-B observation).

**Phase 3 (refresh persists cancellation-pending UI — THE customer-trust gap closure)**: PASS. Browser refresh of `/account?s=...&sig=...` → page renders "Cancellation pending — access through June 16, 2026" + Reactivate button. NOT the broken pre-F3.2.1 behavior of rendering "Active + Cancel button". D6 early-precedence branch firing correctly.

**Phase 4 (60s wait — state persists; no period_end webhook expected)**: PASS. D1 query after 60s shows `cancel_pending_at=1779056419` unchanged + `status='active'`. Confirms F3.2 V-B observation that Dodo doesn't fire on cancel_flag flips; persistent column is the source of truth across the entire cancel-pending window (operationally the remainder of the billing period).

**Phase 5 (reactivate round-trip — THE HIGH-1 symmetric trust-gap close verification)**: PASS. Reactivate click → modal Confirm → POST /api/account/reactivate → 200 + UI client-side flip to "Reactivated — renews June 16, 2026". D1 query confirms `cancel_pending_at=NULL`, `cancelled_at=NULL`, `status='active'`. If the §3.2 Edit B short-circuit gating had been missing, the route would have short-circuited on status='active' and the UPDATE clear would never have fired (cancel_pending_at would have remained populated). It cleared cleanly → HIGH-1 closed end-to-end.

## Inline absorptions (CLI v2 review → execution)
- **MED-NEW-2 (V-C SELECT shape overstated)**: kept reactivate SELECT narrow (3 columns total: status + current_period_end + cancel_pending_at) instead of the 7-column shape from prompt snippet. No bloat introduced.
- **LOW-NEW-1 (if → else if conversion)**: explicit conversion of existing `if (sub.status === "active")` to `else if` after inserting the new early-precedence branch. Without this, both branches would fire and the second would overwrite the first's stateLabel + actionMode assignments (TS wouldn't catch; would manifest as Phase 3 failure). Caught at edit-time.
- **NIT count discrepancies in prompt** ("5 UPDATEs" actual is 4; "6 sites" doesn't cleanly map): cosmetic; ignored.

## Audit-discipline grep results
- §7.1 No Co-Authored-By trailers in F3.2.1 commits: PASS
- §7.2 cancel_pending_at touched in 5 expected source files (cancel route + reactivate route + dodo-webhook + account page + migration): PASS
- §7.3 Reactivate short-circuit `cancel_pending_at === null` gate present at reactivate route line 41: PASS (HIGH-1 close)
- §7.4 Reactivate short-circuit return preserves `next_billing_date: sub.current_period_end` at line 45: PASS (MED-NEW-1 close)
- §7.5 All 4 cancel_pending_at UPDATEs are single-statement (no SELECT-then-UPDATE races): PASS

## Commits
- `cb04633` citation-tracking: F3.2.1 migration 0007 - cancel_pending_at column
- `0cc7b04` marketing-site: F3.2.1 cancel-pending state persistence + reactivate symmetric trust-gap close

Both commits clean — no `Co-Authored-By: Claude` trailer.

## Convergence trajectory

5 rounds total: spec v1 → CLI v1 review → spec v2 → deploy-prompt v1 → CLI v2 review (added v2 spec review separately) → deploy-prompt v2 → execution. Within micro-slice tier flex band (3-5).

**Pattern data point**: F3.2.1 is the second consecutive micro-slice to land at upper-band edge (F3.0.1 at 4; F3.2.1 at 5). Both surfaced latent symmetric-coverage gaps at CLI v1 review (F3.2.1's was reactivate's already-active short-circuit becoming a trust gap once `cancel_pending_at` exists). Hypothesis: micro-slices with cross-route symmetric-state implications consistently surface this class of gap at v1 review — useful framing for spec authors to pre-scan symmetric branches before locking.

## V-D2 observation (locked at F3.2.1 execution; carries forward)

F3.2 V-B confirmed Dodo doesn't fire `subscription.cancelled` on flag flips. F3.2.1 Phase 4 confirms this operationally for an extended wait window: state persists with no spontaneous webhook arrival. **D4 (webhook handler clear) is functionally dead code during normal cancel-then-reactivate cycles** — fires only at actual period_end (1-30 days post-cancel-click depending on billing cycle). D3 (reactivate route clear) is the operative path during active subscription lifetime.

Locked observation: D4 + D5 webhook clears are correct belt-and-suspenders for the period_end + actual-reactivate-webhook scenarios. Don't remove them; their presence is the audit trail that cancel_pending_at is correctly torn down across ALL paths that mutate canonical D1 state.

## Pending operator action

1. F3.2.1 unblocks the pre-public-launch gate-revert sequence (the "cancel anytime" compliance gap was the last subscriptions-layer blocker)
2. Phase 6+ verification not applicable for F3.2.1 (no welcome-email or onboarding flow changes)
3. F3.3 round-2 cleanup commits (`fe33381` + `1468581`) landing alongside this ship-report cover the wordmark hosted asset + email header simplification

## Memory candidates queued (post-ship)

1. **Symmetric-coverage gap pattern in micro-slices**: F3.2.1 v1 spec proposed cancel-side persistence but missed that the existing reactivate-side short-circuit would silently no-op for the new pending state. CLI v1 review caught it via integration-flow V-read of the full reactivate control-flow (not just the post-PATCH insertion point). Memory candidate: "for micro-slices that introduce a new D1 state column, V-read every existing branch that filters on the existing columns the new column is supplementing — they're potential silent-no-op sites for the new state."

2. **Dodo cancel_flag-flip non-firing webhook locked observation**: Phase 4 operationally re-confirms F3.2 V-B for the F3.2.1 60s window. Pattern: external payment providers that distinguish "intent-to-cancel" (a flag toggle) from "actual cancellation" (period_end transition) may webhook only on the latter, not the former. Persistent local state is the right tool to bridge.

3. **Deploy-prompt V-read assertion drift**: F3.2.1 deploy-prompt v2 V-C overstated the actual SELECT shape (claimed 6 columns; real was 2). Inline absorption at execution-time kept the edit minimal. Lesson: V-read assertions in deploy-prompts can drift from reality if the prompt author works from memory rather than current code. Operator must trust V-read result over prompt assertion when they differ; prompt snippets are illustrative only.

## Total execution wall-time
~25 minutes (V-reads + migration + 4 source edits + typecheck + deploy + 5 verification phases + 2 commits + audit greps). Faster than the prompt's 30-45min estimate — micro-slice tier with no cross-package coordination, plus all 5 phases passed first-try (no debugging loops).
