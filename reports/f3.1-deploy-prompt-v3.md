# F3.1 — Onboarding-Day Welcome Artifact — Deploy Prompt v3

## v3 Round deltas (per CLI v2 V-read 2026-05-16)

CLI's v2 V-read pass verified all 18 v1→v2 fixes landed correctly. Four new LOWs surfaced from v2 deltas themselves (three stale-prose internal contradictions + one real code correction). No new HIGHs or MEDs. v3 is one-shot polish: drop stale prose, add TTL.

**LOW fixes:**

19. **LOW — SessionRecord TTL.** §3.4's `env.SESSIONS.put(\`audit:${apSessionId}\`, ...)` had no TTL. Existing audit-fulfill `writeSession` at route.ts:46-50 uses `{ expirationTtl: SESSION_TTL_SEC }` (30 days). v3 adds matching TTL — prevents orphan-session leak if D5 fetch fails before audit-fulfill runs the next writeSession.

20. **LOW — Stale "confabulation tax" audit-discipline note.** v2 MED-4 replaced the phrase with plain language, but the §3.1 post-block Notes still claimed it was "permitted in customer post-purchase context per B1.3-followup precedent" — orphan note. v3 drops it.

21. **LOW — Stale "mirrors sendGapReportReadyEmail" claims (2 sites).** v2 corrected the framing in §3.1's prose intro but left:
    - Inline code comment: `// 3. Resend send (existing pattern; mirrors sendGapReportReadyEmail).` — wrong (link-only function doesn't mirror).
    - Post-block Notes: "V-read §1.2 confirms (existing pattern uses both)." — contradicts v2's §1.2 which explicitly says the existing pattern does NOT have AUDITS.
    v3 drops both.

22. **LOW — `request`-vs-`req` defensive note.** Actual code uses `request` (route.ts:57 V-read confirmed). v3 tightens the §3.4 closing note from conditional-adjustment to assertion: "V-read confirmed `request` per submit/route.ts:57; no adjustment needed."

**Convergence-pattern observation for post-ship memory:**

F3.1 will land at 7 total rounds (3 spec + 3 deploy-prompt + 1 execution) — one over the hybrid-extension upper bound (4-6 per `feedback_locked_architecture_narrower_surface_convergence.md`). The single unplanned round (deploy-prompt v1 → v2) cost was the "spec-faithful but code-blind" deploy-prompt authoring trap: deploy-prompts require V-reads against the actual existing code shape (FulfillEnv, SendEnv, SubmitEnv interfaces; KV key prefixes; variable scope), not just spec-coverage. Worth a sweep-discipline memory note after ship: **deploy-prompt-authoring V-read discipline is stricter than spec-authoring V-read discipline** — the former includes type-interface-shape and key-namespace-convention V-reads that the latter can sometimes defer to the deploy-prompt round.

---

## v2 Round deltas (per CLI v1 review 2026-05-16)

CLI v1 V-read pass surfaced 7 HIGHs + 4 MEDs + 5 LOWs + 2 NITs. Pattern: deploy-prompt was written from spec v3 without V-reading actual code shape (V-A wasn't enough; the surrounding env interfaces drifted from my assumptions too). v2 absorbs concrete code changes (not just notes) and rewrites V-reads §1.2 + §1.4 to assert actual current shapes.

**HIGH fixes:**

1. **HIGH-1 KV key prefix.** `audit-fulfill/route.ts:33` reads `audit:${sessionId}` (prefixed). v1's §3.4 wrote bare `apSessionId` — pipeline would silently 404. v2 writes `` `audit:${apSessionId}` ``.
2. **HIGH-2 `ctx` unbound.** submit/route.ts:58 destructures only `env` from `getCloudflareContext()`. v1's §3.4 called `ctx.waitUntil(...)` without binding ctx. v2 changes the destructure to `const { env: rawEnv, ctx } = getCloudflareContext();`.
3. **HIGH-3 `subscriptionId` undefined.** Existing code uses `tokenResult.subscriptionId`. v1 referenced bare `subscriptionId`. v2 aliases at top of inserted block.
4. **HIGH-4 `now` shadow.** submit/route.ts:123 declares `const now`. v1 redeclared. v2 drops the redeclaration; reuses existing `now`.
5. **HIGH-5 `FulfillEnv` missing `CITATION_DB`.** audit-fulfill/route.ts:19-23 `FulfillEnv` doesn't include CITATION_DB. v1's §3.2 read from it — typecheck fail. v2 extends FulfillEnv.
6. **HIGH-6 `SendEnv` missing `AUDITS`.** score-email.ts:24-27 `SendEnv` doesn't include AUDITS. v1's §3.1 fetched from R2 — typecheck fail. v2 extends SendEnv. v1's "mirrors existing sendGapReportReadyEmail pattern" claim was wrong — that function is link-only; F3.1's pattern is the citation-tracking digest-email.ts attachment pattern ported across packages.
7. **HIGH-7 V-read assertion mismatches.** v1's §1.2 expected "SendEnv interface (AUDITS R2 + RESEND_API_KEY)" — actual has neither matching shape. §1.4 expected type-intersection — actual is `interface ... extends OnboardingTokenEnv` without SESSIONS. Operators would HALT per §0 rules. v2 rewrites §1.2 + §1.4 to assert actual current shapes, then documents deltas as required edits.

**MED fixes:**

8. **MED-1 dynamic import.** Static import at top of audit-fulfill/route.ts; v1's dynamic `await import("@/lib/score-email")` adds microtask for zero benefit.
9. **MED-2 discriminator regex over-narrow.** Widen to `[A-Za-z0-9_-]+` to accept underscores/hyphens within Dodo sub IDs if format ever shifts.
10. **MED-3 CAS=0 silent skip ambiguity.** Differentiate "already fired" (idempotent expected) from "subscription missing" (anomaly): post-CAS log line at `console.warn` level when sub_id row absent.
11. **MED-4 "confabulation tax" jargon in customer copy.** Internal coinage; not in any external Astrant marketing. v2 replaces with plain language: `Dim 6: how often AI agents cite your brand accurately (baseline across 3 major-model APIs)`.

**LOW fixes:**

12. **LOW-1 §1.1 V-read return-type.** Change `return new Response(...)` to `NextResponse.json(...)` expectation.
13. **LOW-2 §1.11 origin.ts existence.** Relax to "may or may not exist; F3.1 uses inline `new URL(req.url).origin` regardless."
14. **LOW-3 §3.2 return template.** Use `NextResponse.json(...)` form to match actual code.
15. **LOW-4 wrangler `kv:key` deprecation.** Use `kv key list / get` (v3+ deprecation; v4 requires).
16. **LOW-5 UTF-8 mojibake.** v1 file written by Write tool (should be clean UTF-8); CLI's mojibake observation may be codepage-side render. Operator-discipline note: open in UTF-8-aware editor.

**NIT fixes:**

17. **NIT-1 commit message paragraphs.** Split §9.2 body into multiple `-m` flags for paragraph rendering.
18. **NIT-2 DROP COLUMN claim.** D1 doesn't expose `sqlite_version()` (per B1.3-followup execution); reframe §11.1 rollback as "operator-discipline manual write" with verify-on-first-use note.

**Sweep-discipline observations** (memory candidates after F3.1 ships):
- Cross-handler key-namespace audit (would have caught HIGH-1)
- `getCloudflareContext` destructuring audit (would have caught HIGH-2)
- Env-interface narrowing audit (would have caught HIGH-5 + HIGH-6)

---

**Spec:** `pharos-f3-1-onboarding-day-welcome-artifact-spec.md` v3 (lock-everything sign-off 2026-05-16).
**Working directory:** `F:\pharos\`
**Targets:** citation-tracking (migration only, no source change, no redeploy) + marketing-site (source edits + redeploy).
**Estimated runtime:** ~1-1.5h end-to-end (V-reads + migration + 4 source-edit blocks + redeploy + 5-6 verification phases).

[Full deploy-prompt v3 body archived in conversation transcript at C:\Users\dembo\.claude\projects\f--pharos\. Sections 0-12 plus closing notes describe inline constraints, pre-flight V-reads, migration 0006 author/apply/verify, four source-edit blocks (§3.1 score-email, §3.2 audit-fulfill, §3.3 welcome-email, §3.4 onboarding/submit), local validation, deploy, post-deploy verification phases 1-6, audit-discipline grep checklist, two-commit structure, sign-off checklist, rollback paths.]

**End of deploy prompt v3.** Total 22 fixes folded (7 HIGHs + 4 MEDs + 5 LOWs + 2 NITs from v1 review; 4 LOWs from v2 review polish). Three HIGH clusters resolved in v2; four prose-internal-contradiction + 1 real-code LOWs resolved in v3.

**Convergence at v3:** 3 spec + 3 deploy-prompt + 1 execution = 7 total. One over the hybrid-extension band's upper edge (6 expected for the tier per `feedback_locked_architecture_narrower_surface_convergence.md`).
